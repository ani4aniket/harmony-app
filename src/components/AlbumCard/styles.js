import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  cardBody: {
    width: "28%",
    borderTopWidth: 0,
    borderRadius: 6,
    height: "28%",
  },
  image: {
    flex: 1,
    height: 90,
    borderRadius: 10,
  },
});

export default styles;
